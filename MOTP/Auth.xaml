<Window x:Class="MOTP.Auth"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MOTP.ViewModel"
        xmlns:Menu="clr-namespace:MOTP.Utilities"
        mc:Ignorable="d"
        Title="Авторизация"
        Height="120"
        Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#212529">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="TB_Pas" 
            Style="{DynamicResource TextBoxApp}" 
            Width="NaN" Margin="10,34,10,0" 
            ToolTip="Тип" Height="30" HorizontalAlignment="Stretch" VerticalAlignment="Top" KeyDown="TB_Pas_KeyDown"/>

        <TextBlock 
            Text="Пароль:"
            Foreground="#DBDBDB"
            FontSize="20"
            VerticalAlignment="Top"
            FontFamily="/Fonts/#Rubik Medium" 
            Height="30" Width="480" 
            HorizontalAlignment="Left" Margin="10,4,0,0" />

    </Grid>
</Window>
